import React from 'react';
import SearchBar from '../components/SearchBar';

const Header = () => {
    return(
        <header>
            <SearchBar />
        </header>
    )
}

export default Header;